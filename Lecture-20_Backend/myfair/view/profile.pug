extend base.pug
block content
  .profile-container
    h2 User Page
    p Name :#{user.name}
    p Email:#{user.email}
    p
      a(herf="/") Settings
    if user.role=="admin"
      p 
        a(href="/managePlans" ) Manage Plans
      p 
        a(href="/manageUser") Manage User  
                  